name: kinkajou
channels:
  - defaults
dependencies:
  - pip >= 22.2.2
  - python >= 3.10.6
  - setuptools >= 63.4.1
  - coverage >= 6.3.0
  - pytest >= 7.1.2
  - sphinx >= 5.0.2
prefix: C:\Users\M\.conda\envs\kinkajou
